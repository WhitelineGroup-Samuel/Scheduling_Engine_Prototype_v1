# =============================================================================
# Whiteline SportsHub — Scheduling Engine (Back-End)
# Environment Template (copy to ".env" for local development)
# =============================================================================

# --- Application environment ---
# One of: dev | test | prod
APP_ENV=dev

# Log level: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO

# Emit JSON logs? (true/false)
LOG_JSON=false

# Local timezone for presentation (store UTC in DB)
TIMEZONE=Australia/Melbourne

# Optional path for exporting files/results (created if missing)
EXPORT_DIR=./exports

# --- Database connectivity ---
# Choose ONE of the following approaches:

# (A) Provide a full SQLAlchemy URL (preferred in prod)
# DATABASE_URL=postgresql+psycopg://USER:PASS@HOST:5432/DBNAME

# (B) Or provide parts; the app will construct DATABASE_URL if it's not set
DB_HOST=localhost
DB_PORT=5432
DB_NAME=scheduling_dev
DB_USER=scheduler_user
DB_PASSWORD=changeme

# Notes:
# - In dev/test, the app will attempt to load this .env file.
# - In prod, this file is ignored; set OS environment variables instead.
# =============================================================================
